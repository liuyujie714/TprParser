[tool.cibuildwheel]
before-build = "cd TprParser"

[build-system]
requires = ["setuptools>=42", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[tool.hatch.build]
include = ["TprParser/*"]
